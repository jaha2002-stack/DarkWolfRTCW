name: DarkWolf DXR HalfRes Cache Build

on:
  workflow_dispatch:
  push:
    paths:
      - 'src/**'
      - 'patches/**'
      - 'scripts/**'
      - 'test-bats/**'
      - '.github/workflows/darkwolf-dxr-halfres-cache-build.yml'

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v2

      - name: Apply DXR HalfRes cache patches
        shell: pwsh
        run: ./scripts/apply-dxr-halfres-cache-full.ps1

      - name: Build clean runtime release
        shell: pwsh
        run: ./scripts/build-windows-clean-release.ps1 -Configuration Release -Platform x64

      - name: Upload runtime artifact
        uses: actions/upload-artifact@v4
        with:
          name: DarkWolf-DXR-HalfRes-Cache-Release
          path: dist/**
          if-no-files-found: error
